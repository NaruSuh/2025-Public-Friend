═══════════════════════════════════════════════════════════════
  학회 참석자 만족도 조사 보고서 자동 생성기
  - 아침까지 보고서 완성 시키기! -
═══════════════════════════════════════════════════════════════

📋 한 줄 요약
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
설문조사 CSV/Excel 넣으면 → 통계 분석 + AI 텍스트 → 완성된 보고서 나옴!


🚀 초간단 3단계
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

1단계: 패키지 설치
  $ cd ~/survey-report-generator
  $ pip install -r requirements.txt

2단계: API 키 설정
  $ export ANTHROPIC_API_KEY='sk-ant-api03-...'

3단계: 보고서 생성!
  $ python generate_report.py --data data/sample_survey.csv --config config.json

끝! output/ 폴더에 보고서 생성됨!


📁 당신의 데이터로 보고서 만들기
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Step 1) 설문조사 데이터를 data/ 폴더에 넣기
  예: data/my_survey.csv

Step 2) config.json 수정
  - conference_name: 학회 이름
  - score_columns: 숫자 평점 컬럼명들 (예: [overall_satisfaction, quality])
  - column_labels: 컬럼명 → 한글 표시명 매핑
  - text_columns: 주관식 응답 컬럼명

  ⚠️ 중요: 컬럼명은 CSV 파일의 실제 헤더와 정확히 일치해야 함!

Step 3) 명령 실행
  $ python generate_report.py --data data/my_survey.csv --config config.json


💡 주요 명령어
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

# 샘플로 테스트
python generate_report.py --data data/sample_survey.csv --config config.json

# 워드 형식으로 생성 (추천! 바로 제출 가능)
python generate_report.py --data data/survey.csv --config config.json --format docx

# HTML 형식으로 생성
python generate_report.py --data data/survey.csv --format html

# 출력 경로 지정
python generate_report.py --data data/survey.csv --output ~/Desktop/report.md

# Excel 파일 사용
python generate_report.py --data data/survey.xlsx --config config.json

# 워드 파일에서 데이터 읽기 (먼저 CSV로 변환)
python convert_docx_to_csv.py data/survey.docx data/survey.csv
python generate_report.py --data data/survey.csv --config config.json


📊 생성되는 보고서 내용
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

✓ 경영진 요약 (AI 생성)
✓ 전체 만족도 지수 (0-100 점수)
✓ 항목별 상세 통계 (평균, 분포, 그래프)
✓ 각 항목의 AI 분석 텍스트
✓ 주관식 응답 AI 인사이트
✓ 데이터 기반 개선 권장사항
✓ 결론 및 향후 전망
✓ 상세 통계 데이터 (JSON)


🔧 문제 해결
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Q: "ANTHROPIC_API_KEY가 설정되지 않았습니다"
A: export ANTHROPIC_API_KEY='your-key' 실행

Q: "컬럼 'xxx'을 찾을 수 없습니다"
A: config.json의 컬럼명이 CSV의 실제 헤더와 일치하는지 확인
   CSV 헤더 확인: head -1 data/your_file.csv

Q: "한글이 깨져요"
A: CSV를 UTF-8로 저장하거나 Excel(.xlsx) 사용

Q: "설치가 안돼요"
A: pip install --upgrade pip
   pip install -r requirements.txt


📚 더 자세한 정보
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

README.md - 전체 문서
QUICKSTART.md - 빠른 시작 가이드
example_usage.py - 코드 사용 예시


🎯 꿀팁
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

1. 먼저 샘플 데이터로 테스트해보기
2. config.json 복사해서 백업 만들기 (cp config.json config_backup.json)
3. 데이터 파일의 헤더(첫 줄) 먼저 확인하기
4. 한 번에 안되면 score_columns 하나씩 추가해보기
5. API 키는 .bashrc에 저장해두면 편함


═══════════════════════════════════════════════════════════════
이제 아침까지 보고서 걱정 끝! 🎉
문제 있으면 README.md 참고하세요!
═══════════════════════════════════════════════════════════════
